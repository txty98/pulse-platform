export default function NoTenantPage() {
  return (
    <main className="page-shell">
      <section className="banner">
        <p>This account is authenticated, but it does not have an active tenant membership yet.</p>
      </section>
    </main>
  );
}
